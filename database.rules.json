{
    "rules": {
        ".read": true,
        ".write": true,
         "messages" : {
            "$app_id" : {
                "$user_id" : {
                  ".indexOn" : "sentOn",
                    ".write": "$user_id === auth.uid",
                    ".read": "$user_id === auth.uid"
                }   
              }
       }
    }
}